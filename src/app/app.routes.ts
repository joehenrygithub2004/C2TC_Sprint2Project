import { Routes } from '@angular/router';
import { UnitPageComponent } from './features/unit/unit-page.component';

export const routes: Routes = [
  { path: '', component: UnitPageComponent }
];

